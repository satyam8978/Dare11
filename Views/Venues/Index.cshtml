@model IEnumerable<CricketBooking.Models.venue>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}



<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table bg-white">
    <tr>
       
        <th>
 Location
        </th>
        <th>
         Venue
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
       
        <td>
            @Html.DisplayFor(modelItem => item.slname)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.sname)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.iId }) |
            @Html.ActionLink("Details", "Details", new { id=item.iId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.iId })
        </td>
    </tr>
}

</table>
